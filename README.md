* Comando para criar e construir o executável do projeto
```bash
cmake -S . -B
```
```bash
cmake --build build
```

* Comando para rodar o executável a partir da raiz do diretório
```bash
./build/sandbox
```